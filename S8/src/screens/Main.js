import React, {Component } from 'react';

import {
    SafeAreaView,
    ScrollView,
    StatusBar,
    StyleSheet,
    Text,
    useColorScheme,
    View,
    Button, TextInput, TouchableOpacity, Alert,
} from 'react-native';


export class Main extends Component{
    render(){
        return(
            <View>
                <Text>
                    xxxxx
                </Text>
            </View>
        );
    }
}